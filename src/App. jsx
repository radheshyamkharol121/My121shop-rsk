// src/App.jsx
import { Route, Routes, Navigate } from 'react-router-dom';
import Navbar from './components/Navbar.jsx';
import Footer from './components/Footer.jsx';
import Home from './pages/User/Home.jsx';
import ProductDetails from './pages/User/ProductDetails.jsx';
import Cart from './pages/User/Cart.jsx';
import Checkout from './pages/User/Checkout.jsx';
import Login from './pages/Auth/Login.jsx';
import Signup from './pages/Auth/Signup.jsx';
import AdminLogin from './pages/Auth/AdminLogin.jsx';
import AdminProducts from './pages/Admin/Products.jsx';
import ProductCreate from './pages/Admin/ProductCreate.jsx';
import ProductEdit from './pages/Admin/ProductEdit.jsx';
import { useAuthState } from './context/AuthContext.jsx';

// ProtectedRoute: user logged in होना चाहिए
function ProtectedRoute({ children }) {
  const { user, loading } = useAuthState();
  if (loading) return <div className="p-8 text-center">Loading...</div>;
  return user ? children : <Navigate to="/login" replace />;
}

export default function App() {
  return (
    <div className="min-h-screen flex flex-col">
      <Navbar />
      <main className="flex-1 container mx-auto px-4 py-6">
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/product/:id" element={<ProductDetails />} />
          <Route path="/cart" element={<Cart />} />
          <Route path="/checkout" element={<ProtectedRoute><Checkout /></ProtectedRoute>} />
          <Route path="/login" element={<Login />} />
          <Route path="/signup" element={<Signup />} />

          {/* Admin */}
          <Route path="/admin/login" element={<AdminLogin />} />
          <Route path="/admin/products" element={<AdminProducts />} />
          <Route path="/admin/product/create" element={<ProductCreate />} />
          <Route path="/admin/product
/edit/:id" element={<ProductEdit />} />
        </Routes>
      </main>
      <Footer />
    </div>
  );
}


// 📂 src/App.jsx

import Signup from "./pages/Auth/Signup"; // ✅ नया import

function App() {
  return (
    <div className="min-h-screen flex flex-col">
      <Navbar />
      <main className="flex-1 container mx-auto p-4">
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/login" element={<Login />} />
          <Route path="/signup" element={<Signup />} /> {/* ✅ नया signup route */}
          <Route path="/cart" element={<Cart />} />
          <Route path="/admin/products/edit" element={<ProductEdit />} />
        </Routes>
      </main>
      <Footer />
    </div>
  );
}